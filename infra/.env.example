# =============================================================================
# SonarQube Code Janitor - Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your actual values
# DO NOT commit the .env file to version control!
# =============================================================================

# -----------------------------------------------------------------------------
# SonarQube Configuration (REQUIRED)
# -----------------------------------------------------------------------------
# URL of your SonarQube instance
# For local deployment (using docker-compose): http://sonarqube:9000 (from backend) or http://localhost:9000 (from host)
# For external SonarQube: https://sonarqube.yourcompany.com
SONARQUBE_URL=http://sonarqube:9000

# SonarQube authentication token
# For local deployment: Use the setup script to generate (./setup-sonarqube.sh)
# For external: Generate at SonarQube > My Account > Security > Generate Tokens
# Default admin credentials: admin/admin (change on first login!)
SONARQUBE_TOKEN=your-sonarqube-token-here

# The project key to monitor in SonarQube
# For local deployment: Create project in SonarQube UI or use setup script
SONARQUBE_PROJECT_KEY=my-project

# -----------------------------------------------------------------------------
# GitHub Configuration (REQUIRED)
# -----------------------------------------------------------------------------
# GitHub Personal Access Token with repo permissions
# Generate at: GitHub > Settings > Developer settings > Personal access tokens
GITHUB_TOKEN=your-github-token-here

# GitHub repository owner (username or organization)
GITHUB_REPO_OWNER=your-github-username

# GitHub repository name
GITHUB_REPO_NAME=your-repo-name

# Default branch to create PRs against
GITHUB_DEFAULT_BRANCH=main

# -----------------------------------------------------------------------------
# OpenAI Configuration (REQUIRED)
# -----------------------------------------------------------------------------
# OpenAI API key for code generation
# Get from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your-openai-api-key-here

# OpenAI model to use for code generation
# Options: gpt-4, gpt-4-turbo-preview, gpt-3.5-turbo
OPENAI_MODEL=gpt-4

# -----------------------------------------------------------------------------
# Fixer Service Configuration (OPTIONAL)
# -----------------------------------------------------------------------------
# Enable automatic fixing of new issues (true/false)
# If false, fixes must be triggered manually via the dashboard
AUTO_FIX=false

# How often to poll SonarQube for new issues (in seconds)
POLL_INTERVAL_SECONDS=60

# -----------------------------------------------------------------------------
# Database Configuration (OPTIONAL)
# -----------------------------------------------------------------------------
# PostgreSQL connection string
# Default uses the docker-compose postgres service
# DATABASE_URL=postgresql://postgres:postgres@postgres:5432/sonarqube_codex
